1、定义一个数组，初始化10个不重复的整数，然后求出数组中的最大数、最小数、所有数之和以及平均值。

2、看程序说结果，注意不要提前运行代码

```javascript
var s = 30;
var i = 50;
s += i;
document.write("s="+s); //  s=80
var x = 0;
var y = 0;
var z = 0;
var b;

b = x>0 && z++>1;
x++;
document.write("b="+b); // b=fales
document.write("z="+z); // z=fales

b = x>0 || z++>1;
document.write("b="+b); // b=fales
document.write("z="+z); // z=fales
```

3、打印1到100之内的整数，但数字中包含9的要跳过（十位包含9，个位包含9）例如 1 2 3 4 5 ....

4、定义一个方法,功能为:计算1~n之间的奇数的平均数,并将其结果返回.